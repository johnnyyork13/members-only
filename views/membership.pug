extends layout 

block content 
  h1 Become a member today, #{user.displayName}!
  h3 Just find the secret word in this repo's README file. 
  h3 My repo can be found 
    a(href='https://www.github.com/johnnyyork13/members-only' target="_blank") here!
  br
  form(action="/secret" method="post")
    label(for="secretWord") Enter Secret Word Here: 
    input(type="text" name="secretWord" required="true")
    button Check Secret
  if (showModal)
    section(class="modal-background")
      section(class="modal")
        if (!showError)
          span
            p(class="modal-p") Congratulations! 
            p(class="modal-p") You now have member priveledges. 
          a(href="/") Back Home
        else 
          p Incorrect secret word... 
          a(href="/membership") Try Again